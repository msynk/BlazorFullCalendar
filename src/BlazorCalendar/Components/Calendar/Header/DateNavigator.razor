<div style="display:flex;align-items:center;gap:4px;">
    <button class="cal-btn cal-btn-icon cal-btn-sm cal-btn-nav" @onclick="State.NavigatePrevious" title="@Texts.PreviousButtonTitle">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
    </button>
    <span style="font-size:14px;font-weight:600;min-width:180px;text-align:center;">
        @BlazorCalendarHelpers.RangeText(State.View, State.SelectedDate, State.Culture)
    </span>
    <button class="cal-btn cal-btn-icon cal-btn-sm cal-btn-nav" @onclick="State.NavigateNext" title="@Texts.NextButtonTitle">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
    </button>
</div>

@code {
    [CascadingParameter] public BlazorCalendarState State { get; set; } = default!;
    [CascadingParameter] public BlazorCalendarTexts Texts { get; set; } = default!;
}
